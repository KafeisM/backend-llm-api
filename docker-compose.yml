services:
  api:
    build: .
    container_name: backend-llm-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=8080
      - DATABASE_URL=sqlite:///./nuria.db
    restart: unless-stopped
